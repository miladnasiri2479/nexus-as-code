---
# External role dependencies
# No external dependencies required - all roles are self-contained.
roles: []
collections:
  - name: community.docker
    version: ">=3.0.0"
  - name: community.general
    version: ">=6.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
