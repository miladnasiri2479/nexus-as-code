---
# roles/backup/defaults/main.yml
# All variables are securely managed in group_vars/all.yml
# Do not store secrets here.

# Control variables for execution (these are runtime flags, not secrets)
run_backup_now: false
restore_backup_file: ""
