---
galaxy_info:
  role_name: storage_fs
  description: Configure local filesystem storage for Nexus
  author: nexus-as-code
  license: Apache-2.0
  min_ansible_version: "2.14"
  galaxy_tags: [nexus, storage, filesystem]
dependencies:
  - role: common
