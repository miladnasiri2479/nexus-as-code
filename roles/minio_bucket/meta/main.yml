---
galaxy_info:
  role_name: minio_bucket
  description: Create and manage MinIO buckets for Nexus
  author: nexus-as-code
  license: Apache-2.0
  min_ansible_version: "2.14"
  galaxy_tags: [minio, buckets]
dependencies:
  - role: minio
